<div class="mt-2 space-y-1 text-xs sm:text-sm text-gray-800">
    <p><span class="font-semibold">{{ number_format($followersCount) }}</span> Seguidores</p>
    <p><span class="font-semibold">{{ number_format($followingCount) }}</span> Siguiendo</p>
    <p><span class="font-semibold">{{ number_format($postsCount) }}</span> Publicaciones</p>
</div>